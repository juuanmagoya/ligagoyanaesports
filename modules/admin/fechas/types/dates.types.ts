export interface Date {
    id: string;
    name: string;
    created_at: string;
    updated_at: string;
}

export interface CreateDateData {
    name: string;
}

export interface UpdateDateData {
    name: string;
}